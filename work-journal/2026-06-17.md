# 2026-06-17 work log

project-docs 시스템을 Holygrail(Godot 4.6 게임) 프로젝트에 bootstrap-project 1회 셋업 — placeholder 치환 + 문서 정비.

## 미완 / 다음

- `PROJECT_DESC`는 "Godot 4.6 기반 게임 프로젝트 (컨셉 미정)" 잠정값 — 게임 컨셉 확정 시 갱신.
- 인프라/서버 관련 절(overview §1.2~6, cicd 전체, code-style §2)은 "현재 미사용"로 보존 — 서버 기능 도입 시점에 채운다.
- 첫 실제 TODO 진입 시 `backlog/data/{TRACK}/` 디렉토리는 `cp` 시점에 생성(현재 미생성).

## 1. bootstrap-project 셋업

**결론**: Discovery → 토큰 치환(11파일) → 문서 채우기(경량) → 검증까지 4단계 완료. broken link 0 · undocumented 0 · 미치환 placeholder 0(README 토큰표·bootstrap 스킬 문서의 `{{}}`만 의도적 잔존).

확정 토큰값: `PROJECT_NAME=Holygrail`, `WORKSPACE_ROOT=/home/hansol/workspace/holygrail`, `REPO_LIST=project-holygrail (Godot 4.6 게임), project-docs (문서 시스템)`, `GITOPS_REPO=N/A`, `SERVICE_DOMAIN=N/A`.

탐지 스택: GDScript / Godot 4.6 (Jolt Physics 3D, `gl_compatibility` 렌더), 에셋 Git LFS(`*.mp4/mov/wav/ogg`). 현황은 스모크 테스트(스프라이트 4방향 이동, `main_00.tscn`).

## 2. 트랙 체계 확정

**결론**: TODO 트랙 = `GAMEPLAY, ART, AUDIO, ENGINE, BUILD` (게임 도메인 분할), ADR 트랙 = `GAMEPLAY, TECH, PROCESS`.

ADR(광범위 묶음)↔TODO 매핑: TECH = ENGINE+BUILD 포괄, GAMEPLAY = 게임설계(GAMEPLAY/ART/AUDIO 포괄), PROCESS = 협업·문서. 향후 서버 추가 시 `SERVER` 트랙 신설 예정. backlog/done INDEX 섹션과 `workitem/README.md`·`decisions/template.md`에 트랙 범위표 기입.

## 3. 보존 전략 (웹/k8s 전제 절)

**결론**: 스켈레톤이 웹/마이크로서비스/GitOps용이라 게임에 안 맞는 절이 많으나, **삭제 대신 "현재 미사용 — 향후 [조건] 시 작성"으로 보존**(seam 유지, YAGNI). 사용자 결정 — 서버 기능 추가 가능성 대비.

대상: `overview.md` §1.2~6(GitOps·데이터·인프라·라우팅), `cicd.md` 전체(현재 CI 없음, Godot export 수동), `code-style.md` §2 레이아웃(app/+Docker+k8s)·§2.1/2.2 SOPS secret. `code-style.md` §1에 현재 언어 GDScript 컨벤션(탭, snake_case 함수/파일, PascalCase 클래스/노드) 추가, §3 Naming 표는 비-GDScript(향후 서버) 코드용으로 유지.

## 4. 정리 작업

**결론**: snacks 예시 scaffolding 주석(트랙·경로 치환 안내)을 `CLAUDE.md`·`GEMINI.md` 4본에서 제거, "(bootstrap-project 시 확정)" stale 표기 정리.

⚠️ 워크스페이스 루트 `CLAUDE.md`/`GEMINI.md`는 루트가 git 레포가 아니라 on-disk 반영만(커밋 대상 아님). `project-docs` 레포 내 동명 파일은 커밋 대상. 메타 2파일(`README.md` 토큰표, `tools/skills/bootstrap-project.md`)은 재부트스트랩 기계장치라 `{{}}` 그대로 보존.
