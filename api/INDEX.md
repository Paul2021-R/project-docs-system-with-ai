# API INDEX

`specs/`의 API 정의서 원본(TSV 등)을 모듈별로 재분류한 결과의 진입점이다. 모듈별 정의는 [data/](data/)의 `{MODULE}.md`, 공통 정책은 [POLICIES.md](POLICIES.md), 새 모듈 작성은 [TEMPLATE.md](TEMPLATE.md).

<!-- 모듈을 채울 때마다 아래 표에 행을 추가한다.
     추적 대상은 "코드 구현 진척"이 아니라 "명세 재분류 진척"이다 (원본 TSV → 모듈 md 정돈 여부). -->

| 모듈 | 정의 | 우선순위 분포 | 상태 |
|---|---|---|---|
| <MODULE> | `data/<MODULE>.md` | P0 n / P1 n / P2 n | [작성 전] |
