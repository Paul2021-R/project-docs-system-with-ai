# Roadmap

본 디렉토리는 향후 진입할 설계·확장 계획을 담는다. **reference-only** — 현재 진행 상태(active task)는 여기 적지 않는다 (그건 `workitem/backlog/INDEX.md`가 SoT).

<!-- 계획 문서는 "지금 하는 일"이 아니라 "조건이 되면 진입할 설계"다. drift 방지를 위해 진행 스냅샷과 분리한다. -->

## 담는 것

- 영역/서비스 분리 설계 (예: `area-separation-plan.md`)
- HA / scaling 단계 설계 (예: `ha-scaling-plan.md`)
- 아직 착수하지 않았지만 방향이 정해진 큰 구조 변경

## 파일명

`<topic>-plan.md` — 영문 kebab-case.
