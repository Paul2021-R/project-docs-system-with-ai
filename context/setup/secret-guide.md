# Secret Guide

워크스페이스에서 secret(`.env` · k8s Secret · 암호화 키 등)을 안전하게 만들고 갱신하는 **실작업 가이드**. 결정 배경("왜")은 해당 ADR, 본 문서는 "어떻게"를 담당한다.

<!-- 프로젝트의 secret 관리 방식에 맞춰 채운다 — 평문 .env / SOPS+age / Vault / 클라우드 secret manager 등.
     관리가 단순한 프로젝트는 §0~§1만 두고 나머지는 삭제한다. -->

---

## 0. 사전 준비

<!-- 필요한 도구·키와 설치 확인 명령. -->

> 🚨 암호화 키 분실 = 해당 키로 암호화된 모든 시크릿 영구 손실. 키 백업은 **git 외부의 별도 보관소**에서만 한다. `.env`·git 커밋에는 절대 들어가지 않는다.

## 1. Secret 생성

<!-- 신규 secret 만드는 단계별 절차. -->

## 2. Secret rotation (값 갱신)

<!-- 기존 secret 값 교체 절차. 무중단 갱신이 필요하면 순서 명시. -->

## 3. 트러블슈팅

<!-- 복호화 실패·권한·인코딩 등 자주 겪는 오류와 처방. -->

## 4. 적용 범위

<!-- 본 가이드가 다루는 secret 종류와 경계(무엇은 다루고 무엇은 안 다루는지). -->
